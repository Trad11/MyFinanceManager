<Window x:Class="MyFinanceManager.Presentation.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MyFinanceManager.Presentation"
        xmlns:transactions="clr-namespace:MyFinanceManager.Presentation.Transactions"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.Resources>
        <DataTemplate DataType="{x:Type local:HomeViewModel}">
            <local:HomeView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type transactions:TransactionViewModel}">
            <transactions:TransactionView />
        </DataTemplate>
    </Window.Resources>
    <StackPanel>
        <ContentControl Content="{Binding CurrentViewModel}" />
        
        <Button Content="Show New Transaction View"
                Command="{Binding ShowTransactionViewCommand}"
                Margin="10"/>
        
        <Button Content="Show New Home View"
                Command="{Binding ShowHomeViewCommand}"
                Margin="10"/>
    </StackPanel>
</Window>
